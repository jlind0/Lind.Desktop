<Window x:Class="Lind.Desktop.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lind.Desktop.Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Page Count:</Label>
            <Label Content="{Binding Pages}"/>
            <Label>Page Size:</Label>
            <TextBox Text="{Binding PageSize}"/>
            <Button Command="{Binding LoadCustomersCommand}">Load</Button>
            <Button Command="{Binding DeleteCustomersCommand}">Delete</Button>
        </StackPanel>
        <DataGrid MaxHeight="360" ItemsSource="{Binding Customers}" AutoGenerateColumns="False">
           
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding FirstName}" Header="First Name"/>
                <DataGridTextColumn Binding="{Binding LastName}" Header="Last Name"/>
                <DataGridCheckBoxColumn Binding="{Binding IsChecked}" Header="Select"/>
            </DataGrid.Columns>
        </DataGrid>

    </StackPanel>
</Window>
